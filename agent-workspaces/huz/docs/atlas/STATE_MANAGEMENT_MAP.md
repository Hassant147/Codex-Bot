# State Management Map

Track global/local state owners, context/store hooks, and data synchronization boundaries.

| State Domain | Source Module | Consumers | Persistence | Notes |
| --- | --- | --- | --- | --- |
| Operator dashboard recent-bookings scope | `Huz-Operator-Frontend/src/api/DashboardApi.js`, `src/pages/Dashboard/components/overview/dashboardViewModel.js`, `src/pages/Dashboard/components/overview/RecentBookingsMiniList.jsx`, `src/pages/Dashboard/Dashboard.jsx` | Operator dashboard recent-bookings card and dashboard source chip | In-memory fetched summary payload only | Batch 04 Phase 1 adds `recentBookingsScope` metadata so the fallback `workflow_bucket=READY` feed is rendered as a ready queue with ready-scope totals instead of being mistaken for cross-bucket recent activity. |
| Operator booking list search query | `Huz-Operator-Frontend/src/pages/Dashboard/BookingsModule/components/Tabs.jsx`, `src/api/BookingApi.js` | Operator booking tabs list view | Local component state only | Batch 04 Phase 1 keeps the debounced search state local but renames the consumer contract to `bookingNumber`, and the UI copy now states that the backend search filters booking numbers only. |
| Web support media href resolution | `Huz-Web-Frontend/src/pages/UserSetting/Support/supportUtils.js` | Web `/help` support history cards | Derived at render time from `API_BASE_URL` plus backend media path | Batch 04 Phase 1 resolves complaint/request attachments and complaint audio against the API origin for split-origin deployments while leaving the support API contract unchanged. |
| Admin booking route identity | `Huz-Admin-Frontend/src/pages/Admin-Panel/Bookings/bookingRouteUtils.js`, `src/App.js` | Admin booking cards, booking detail page, and all admin fulfillment subflow wrappers | URL path params with legacy redirects | Batch 02 Phase 1 removed the global `localStorage("bookingNumber")` handoff; `/booking/:bookingNumber/...` is now the source of truth for selected booking identity, and Phase 2 verified the route wiring with a passing admin build plus loader-based source smoke across the detail/subflow wrappers. |
| Admin booking detail loader | `Huz-Admin-Frontend/src/pages/Admin-Panel/Bookings/BookingDetailsPage/useAdminBookingLoader.js` | `BookingDetails.js`, `UploadEvisa.jsx`, `AirlineTickets.jsx`, `TransportArrangement.jsx`, `HotelArrangement.jsx` | In-memory React context plus route param | Centralizes booking-number parsing and fetch orchestration so admin detail/subflows no longer read `booking` or `bookingNumber` from shared local storage; Phase 2 confirmed all five routed entrypoints still resolve the selected booking through this hook. |
| Admin booking context cache | `Huz-Admin-Frontend/src/context/BookingContext.js` | Admin booking detail, pending/active cards, fulfillment forms | In-memory React context only | The selected booking still lives in context for the current SPA session, but Batch 02 Phase 1 stopped persisting the selected booking and booking number into `localStorage`, reducing cross-tab mix-ups. |
| Admin pending-action availability | `Huz-Admin-Frontend/src/pages/Admin-Panel/Bookings/BookingDetailsPage/Pending.js` | Admin booking detail pending screen | Derived from backend serializer field | The decision form now renders only when `operator_can_act` is true, with read-only messaging tied to payment/traveler readiness when the backend says the booking is not actionable yet. |
| Partner fulfillment mutability gate | `Huz-Admin-Frontend/src/pages/Admin-Panel/Bookings/bookingWorkflowUtils.js`, `Huz-Operator-Frontend/src/utils/bookingDomain.js`, `Huz-Operator-Frontend/src/pages/Dashboard/BookingsModule/BookingDetailsPage/ActiveSatusComponents/shared/BookingSubflowPage.jsx`, `Huz-Backend/booking/manage_partner_booking.py` | Admin/operator completed detail cards and routed fulfillment subflows | Derived from backend `booking_status` plus route guards | Batch 03 Phase 1 makes completed bookings read-only in both partner panels and blocks direct subflow entry unless the booking is in `IN_FULFILLMENT` or `READY_FOR_TRAVEL`; backend document deletion now shares the same gate. |
| Admin reported-traveler visibility | `Huz-Admin-Frontend/src/pages/Admin-Panel/Bookings/BookingDetailsPage/BookingDetails.js`, `src/pages/Admin-Panel/Bookings/BookingDetailsPage/components/ReportedTravelers.js`, `Huz-Backend/booking/serializers.py`, `Huz-Backend/booking/manage_partner_booking.py` | Admin booking detail when `issue_status=REPORTED` | Derived from the legacy detail payload `issue_status` plus additive backend `traveller_detail` compatibility | Batch 03 Phase 1 introduced the admin reported-travelers UI, and Release Gate Phase 1 closed `BOOKING-009` by making the backend legacy detail/report payload expose `traveller_detail` alongside `passport_validity_detail` and by reloading the booking before the report response is serialized. |
